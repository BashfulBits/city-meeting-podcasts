# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling
.pytest_cache/
.ruff_cache/
.wrangler/
workers/city-request-intake/node_modules/
.coverage
htmlcov/

# OS
.DS_Store

# Generated GitHub Pages output (CI regenerates this; keep only .nojekyll).
# Phase 4 may revisit committing docs/ for serving without CI.
/docs/*
!/docs/.nojekyll

# Cross-run build state (audio manifest + change-detection cache). Restored by CI via
# actions/cache, never committed.
/.citypods-state/
