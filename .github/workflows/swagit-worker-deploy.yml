name: Deploy Swagit list Worker

on:
  push:
    branches: [main]
    paths:
      - "workers/swagit-list-proxy/src/**"
      - "workers/swagit-list-proxy/wrangler.jsonc"
      - "workers/swagit-list-proxy/package.json"
      - ".github/workflows/swagit-worker-deploy.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: swagit-list-worker-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "24"

      - name: Test Worker
        working-directory: workers/swagit-list-proxy
        run: npm test

      - name: Deploy Worker
        uses: cloudflare/wrangler-action@9acf94ace14e7dc412b076f2c5c20b8ce93c79cd # v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: workers/swagit-list-proxy
          # Exact pin (matches workers/swagit-list-proxy/README.md), not a floating major -- see
          # that file for the reasoning. Bump deliberately, in one PR, in both places.
          wranglerVersion: "4.129.0"
